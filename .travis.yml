sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t kvenkata986/welcome images/helm-meetup/.
  - docker run -d -p 127.0.0.1:80 kvenkata986/welcome /bin/sh -c "cat /usr/share/nginx/html/index.html"
  - docker ps -a
